-- Домашний адрес соискателя — то же, что уже есть у заведения (companies.address),
-- только с другой стороны. Собираем его вместе с городом при регистрации: не
-- используется ни в одном расчёте сегодня (радиус/геолокация выключены, см.
-- FEATURES.geo), но когда до этого дойдёт, лучше уже иметь данные, чем
-- просить всех заново их вписать.
ALTER TABLE workers ADD COLUMN address TEXT;
