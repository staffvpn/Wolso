-- Отметка последнего напоминания «закройте смену» — тот же класс
-- напоминания, что и у остальных в reminders.ts. closed_by_employer_at
-- (0014) уже говорит, закрыта ли смена; этот столбец только про то,
-- когда в последний раз про это напомнили.
ALTER TABLE applications ADD COLUMN close_reminded_at TEXT;
