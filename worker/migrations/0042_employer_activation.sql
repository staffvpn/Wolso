-- Зеркало signup_reminded_at/photo_reminded_at (0028/0035), но для
-- следующего шага воронки у работодателя: анкета заведения заполнена,
-- а смену так ни разу и не опубликовали. Однозначный сигнал — если не
-- опубликовал ни одной вакансии, набрать команду через Wolso он не мог,
-- так что предположение «у него уже есть свои люди» тут не работает.
ALTER TABLE companies ADD COLUMN never_posted_reminded_at TEXT;
